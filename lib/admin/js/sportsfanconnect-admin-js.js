jQuery(document).ready(function($){
	$('#news_links_catchecklist > li > .selectit > input').attr('type','radio');
	$('#news_links_catchecklist-pop > li > .selectit > input').attr('type','radio');
	
  $('#source_catchecklist > li > .selectit > input').attr('type','radio');
  $('#source_catchecklist-pop > li > .selectit > input').attr('type','radio');

  $('#game_catchecklist > li > .selectit > input').attr('type','radio');
  $('#game_catchecklist-pop > li > .selectit > input').attr('type','radio');

  $('#sched_year_catchecklist > li > .selectit > input').attr('type','radio');
  $('#sched_year_catchecklist-pop > li > .selectit > input').attr('type','radio');

  $('#game_typechecklist > li > .selectit > input').attr('type','radio');
  $('#game_typechecklist-pop > li > .selectit > input').attr('type','radio');
});